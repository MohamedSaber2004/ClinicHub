If you already know the GetHybridSearch implementation in the ClinicHub repository and want an AI/code assistant to perform a deep analysis and enhancement, use this prompt:

Act as a senior .NET Backend Engineer and Google Maps API specialist.

Analyze the entire implementation of the GetHybridSearch feature in the ClinicHub repository.

Current problem:
Some clinics, hospitals, medical centers, and healthcare providers are visible on Google Maps but are not returned by the GetHybridSearch API response.

I need a complete root-cause analysis and implementation improvement.

Tasks:

1. Locate all files involved in:
   - GetHybridSearch endpoint
   - Handlers
   - Services
   - Repositories
   - Google Maps integrations
   - Google Places API requests
   - Geocoding requests
   - Search filters
   - Mapping and DTO projections
   - Caching layers

2. Analyze the entire search flow:
   - Request received
   - Query generation
   - Google API call
   - Result filtering
   - Deduplication
   - Mapping
   - Pagination
   - Final response generation

3. Identify why valid clinics/hospitals are missing:
   - Incorrect Google Places API parameters
   - Radius limitations
   - Location bias issues
   - Pagination token not handled correctly
   - Missing NextPageToken requests
   - Overly restrictive filtering
   - Wrong business type filters
   - Name matching issues
   - Language issues
   - Country restrictions
   - DTO mapping bugs
   - Search result truncation
   - Database merge issues
   - Async execution problems
   - Exception swallowing
   - Cache returning stale results

4. Verify whether:
   - All Google Places pages are fetched
   - NextPageToken is used correctly
   - Nearby Search and Text Search are combined properly
   - Clinics, hospitals, medical centers, laboratories, radiology centers and doctors are all searchable
   - Arabic and English names are supported
   - Search ranking is not removing valid results

5. Improve search quality:
   - Hybrid database + Google Places search
   - Fuzzy matching
   - Multiple medical business types
   - Arabic/English keyword support
   - Score-based ranking
   - Better relevance ordering
   - Duplicate removal using PlaceId
   - Distance-based sorting
   - Intelligent fallback search

6. Add logging:
   - Generated Google request
   - Returned Google count
   - Filtered count
   - Final count
   - Reasons for discarded results

7. Add performance improvements:
   - Reduce unnecessary API calls
   - Optimize LINQ queries
   - Efficient caching strategy
   - Async parallel execution where safe

8. Generate:
   - Root cause report
   - List of discovered issues
   - Recommended fixes
   - Updated production-ready code
   - Explanation of every modification

9. Ensure the final implementation returns the maximum possible number of valid healthcare providers from Google Maps without duplicates while maintaining good performance.

Output:
- Root cause analysis
- File-by-file review
- Exact code changes
- Improved implementation
- Performance considerations
- Testing scenarios
- Edge cases

For even better results, provide the AI with the specific files containing:

GetHybridSearchHandler
Google Places service
Search service
Clinic repository
DTOs and response models